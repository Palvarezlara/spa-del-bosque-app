export default function Footer() {
  return (
    <footer className="bg-dark text-light text-center py-3 mt-5">
      <small>&copy; {new Date().getFullYear()} SPA del Bosque — Mar–Dom 10:00–20:00</small>
    </footer>
  );
}